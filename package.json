{
  "name": "animation",
  "version": "0.1.0",
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "lerna run build --parallel"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^5.1.2",
    "prettier": "^2.7.0",
    "prettier-plugin-tailwindcss": "^0.1.11"
  },
  "private": true
}
